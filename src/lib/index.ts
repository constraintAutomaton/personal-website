// place files you want to import through the `$lib` alias in this folder.

export interface IPublication {
    authors: string[];
    year: number;
    venue: string;
    url: string;
    title: string;
}